const file = @import("file.zig");
const time = @import("time.zig");

pub const FontFilePath = file.FontFilePath;
pub const ConfigFilePath = file.ConfigFilePath;

pub const DecisionTimeMs = time.DecisionTimeMs;
pub const EffectLengthMs = time.EffectLengthMs;
